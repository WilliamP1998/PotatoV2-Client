export class Groups {
  objid: string;
  id: number;
  name: string;

  constructor(objid: string, _id: number, _name: string) {
    this.objid = objid;
    this.id = _id;
    this.name = _name;
  }
}
